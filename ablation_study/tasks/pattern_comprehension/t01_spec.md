# Task: Nested List Flattening

Implement a function that flattens a nested list structure using comprehensions.

**Function signature:**
```python
def flatten(nested: list) -> list:
    """
    Flatten a nested list (one level deep) using comprehensions.

    Args:
        nested: List containing lists and/or individual items

    Returns:
        Flattened list
    """
    pass
```

**Requirements:**
- Use list comprehension
- Handle mix of lists and non-list items
- Preserve order
- Handle empty lists and empty nested lists
- Only flatten one level (don't recursively flatten)
