# Task: Matrix Transpose

Implement a function that transposes a 2D matrix using comprehensions.

**Function signature:**
```python
def transpose(matrix: list[list]) -> list[list]:
    """
    Transpose a 2D matrix.

    Args:
        matrix: 2D list (all rows must be same length)

    Returns:
        Transposed matrix
    """
    pass
```

**Requirements:**
- Use nested list comprehension
- Handle empty matrix
- Handle 1xN and Nx1 matrices
- Assume all rows are the same length
